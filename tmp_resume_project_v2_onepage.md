# 简历 - 项目经历

## 苍穹外卖 — 外卖平台后端

**技术栈**：Spring Boot / MyBatis / ShardingSphere-JDBC / MySQL / Redis / Redisson / RabbitMQ / Elasticsearch

**项目描述**：外卖平台后端系统，覆盖用户下单支付与商家履约流程；在基础业务之上，独立设计并实现高并发防超卖、订单最终一致性、分库分表路由治理、检索与缓存优化等模块。

### 核心工作

- **优惠券抢购高并发防超卖**：针对优惠券抢购场景下的超卖与 Redis/DB 不一致问题，设计“快速失败 + Redisson 按 `couponId` 粒度加锁 + Redis `DECR` 原子扣减 + 锁外异步持久化”链路，并增加对账补偿；JMeter 压测 `1000` 并发抢 `500` 张券，平均 RT `8.95ms`、P99 `107ms`、吞吐 `203 QPS`，验证零超卖且 Redis / DB 数据一致
- **订单链路最终一致性**：针对订单状态变更、MQ 重复投递和 ES 异步同步易产生的数据不一致问题，构建“库存事务强一致 + Outbox 最终一致 + 物流 MQ 状态机校验 + ES 定时对账”四层方案，避免直接双写造成消息丢失、重复消费与索引脏数据
- **分库分表与分片键治理**：针对订单数据增长后的路由与事务扩展问题，基于 ShardingSphere-JDBC 按 `user_id` 拆分为 `2 库 x 8 表` 共 `16` 个分片，并统一关联表分片键，保证订单链路事务不跨库；定位并修复 Snowflake ID 不回写实体导致的下游空指针问题，改为按 `number + userId` 精确回查订单 ID
- **检索与工程优化**：针对菜品高频查询与订单复杂检索场景下的数据库压力和用户体验问题，使用 ES + IK 实现菜品全文检索与管理端订单条件查询，构建“布隆过滤器 + Caffeine + Redis + DB”多级缓存链路，并补齐下单幂等、订单超时双路径兜底与 WebSocket 实时推送，提升链路稳定性与状态反馈时效
